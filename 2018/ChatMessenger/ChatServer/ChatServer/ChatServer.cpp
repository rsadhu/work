#include "ChatServer.h"


ChatServer::ChatServer(QWidget *parent)
	: QMainWindow(parent)
{
	//ui.setupUi(this);
	//connect(ui.pushButton, &QPushButton::pressed, [=]() 
	//{
	//	if (mStateChanged)
	//	{
	//		ui.pushButton->setText("Start");
	//		m_ChatServerPriv.resumeAccepting();
	//	}
	//	else
	//	{
	//		ui.pushButton->setText("Stop");
	//		m_ChatServerPriv.pauseAccepting();
	//	}		
	//	mStateChanged = !mStateChanged;
	//	//m_ChatServerPriv ?  delete m_ChatServerPriv, ui.pushButton->setText("Start") : m_ChatServerPriv = new ChatServerPriv(), ui.pushButton->setText("Stop");
	//});
}
